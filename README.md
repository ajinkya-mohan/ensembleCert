# ensembleCert
Codebase for EnsembleCert, first white-box informed robustness certificate for partition-based ensembles and ScaLabelCert, the first polynomial-time exact certificate for sufficiently wide neural networks against label-flipping poisoning attacks.
